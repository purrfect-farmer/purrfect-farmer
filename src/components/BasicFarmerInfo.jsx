import Alert from "./Alert";

export default function BasicFarmerInfo() {
  return (
    <Alert variant={"warning"}>
      This farmer has basic tasks, tasks are completed automatically in
      background.
    </Alert>
  );
}
